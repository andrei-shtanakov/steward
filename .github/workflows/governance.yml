# Governance caller (ADR-ECO-004 D5). Thin caller: a pinned reference to the reusable gate
# workflow at ai-orchestrators-workspace/.github/workflows/governance-gate.yml@governance-v2
# (the checks it runs live under ci/governance/). No code is copied here. The `governance-v2`
# tag is the pin, kept immutable by the umbrella's `governance-*` tag ruleset (retag/delete
# blocked) — not by git tag semantics, which allow moving a tag.
name: governance
permissions:
  contents: read          # gate only reads (2× checkout of public repos); least-privilege
on:
  pull_request:
  workflow_dispatch:

jobs:
  governance:
    uses: andrei-shtanakov/ai-orchestrators-workspace/.github/workflows/governance-gate.yml@51513e8aa0935e76b3327d6298409a683960a6fb
    with:
      umbrella-ref: "51513e8aa0935e76b3327d6298409a683960a6fb"   # same tag as the uses: pin above
      strict: true
      runtime-scan: true        # runtime code — GOV-003 applies
      authority-guard: false    # holds no authority-root paths
