"""Vendored, pinned copies of cross-repo contract code.

Per the ecosystem rule (vendor pinned copies; never resolve cross-repo paths at
runtime) and BOOTSTRAP.md, steward vendors the small slice of spec-runner's
SpecMeta frontmatter core it consumes rather than depending on the sibling repo.
Files here are hand-pinned copies — do not edit them to change behaviour;
re-vendor when the upstream contract version bumps.
"""
