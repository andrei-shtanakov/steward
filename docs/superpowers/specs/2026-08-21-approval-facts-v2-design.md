# `approval-facts/v2` — внешний контракт merge-акторов и миграция release-гейта

> Дизайн утверждён владельцем 2026-08-21. Запрос — steward#72 (inbox,
> from `dispatcher#agent-merge-observability`). Реализация — по плану, который
> пишется из этой спеки.

## 1. Зачем

`dispatcher#agent-merge-observability` заблокирован: у него нет источника, из которого
видно, кто смержил PR. Оба обходных пути отвергнуты на ревью дизайна и повторять их
запрещено — читать `profiles/approval-policy.yaml` из чекаута steward значит нарушить
границы полирепо (контракт потребляется пиненой копией внутрь, а не импортом из пути
соседа), а воспроизвести `classify_actor` у себя значит завести **два policy engine**,
способных разойтись.

Владелец классификации — steward. Наружу должен выйти переносимый evidence-контракт:
raw facts плюс итоговая классификация плюс provenance применённой политики.

Сегодня `approval-facts` существует только как Python (`src/steward/approvalfacts.py`),
в `contracts/` его нет, вендорить нечего. Задача — **graduation** внутреннего формата в
контракт, а не расширение payload.

Без этого контракта действует ADR-ECO-008 D6: нет наблюдаемости — прогон обязан вести
себя как `merge_authority: human`.

## 2. Границы работы

**Входит:** контракт `contracts/approval-facts/v2/`, переписанный продюсер, эмиссия в
`.steward/`, миграция `check_approval_evidence` на v2, регрессионный набор, migration
acceptance на реальных мержах.

Продюсер, внешний контракт и внутренний гейт — **одна атомарная миграция**. Разнести их
значило бы сознательно оставить две семантики approval-facts, одна из которых не имеет
ни scope, ни completeness, ни lease.

**Не входит:** панель dispatcher и его read-модель (их стадия 2, после вендоринга);
аномалия declared-vs-observed (нужен `merge_authority`, которого сегодня нет нигде);
fsync-долг `verdicts/emitter.py` (отдельный TODO, см. §10).

## 3. Почему v2, а не v1

Текущий формат уже маркирует себя `approval-facts/v1` (`src/steward/approvalfacts.py`,
докстринг модуля), его принимает release-гейт и он зафиксирован live evidence прогона
2026-08-20 (`docs/evidence/2026-08-20-i4-live-acceptance/approval-facts.json`).
Публиковать под тем же именем несовместимую форму значит задним числом переписать то,
что уже доказано.

- новый внешний контракт — `contracts/approval-facts/v2/`;
- CLI после миграции пишет **только** v2;
- гейт читает **только** v2;
- v1 получает документированный статус legacy/internal и **явную ошибку**
  «unsupported legacy approval-facts/v1» — не молчаливую интерпретацию;
- рантайм не поддерживает два формата параллельно.

## 4. Файл

`<repo-root>/.steward/approval_facts.jsonl` — рядом с `gate_verdicts.jsonl`, тот же
бандл, gitignored локальный артефакт.

JSONL выбран не ради симметрии с `gate-verdicts/v1`, а потому что даёт естественную
одну запись на запрошенный элемент, не требует искусственного SHA-ключа для
отрицательных результатов и позволяет изолировать в фикстурах malformed / duplicate /
missing / extra result. Симметрия — приятное следствие: у потребителя уже есть
проверенная дисциплина чтения (`dispatcher/core/governance.py`).

### 4.1 Header

Первая и **только первая** строка.

```json
{"kind":"header","schema_version":"2","repository":"owner/repo",
 "generated_at":"2026-08-21T09:00:00Z","valid_until":"2026-08-22T09:00:00Z",
 "policy_version":1,"policy_digest":"sha256:…","complete":true,
 "scope_sha256":"sha256:…",
 "scope":[{"kind":"pr","value":42},{"kind":"merge_sha","value":"221457933968be9e95acd51d548e080f739c794c"}]}
```

`complete: true` — заявление продюсера, а не доказательство. Читатель **обязан**
проверить его сам (§4.3).

### 4.2 Result

По одной записи на каждый элемент scope.

```json
{"kind":"result","request":{"kind":"pr","value":42},"state":"not_merged","merge_sha":null}
{"kind":"result","request":{"kind":"merge_sha","value":"221457933968be9e95acd51d548e080f739c794c"},
 "state":"merged","merge_sha":"221457933968be9e95acd51d548e080f739c794c",
 "identity":"github:merge-broker","type_hint":"Bot","actor_class":"agent"}
```

**Пять терминальных состояний наблюдения:**

| state | значение |
|---|---|
| `merged` | merge-событие существует, актор получен |
| `not_merged` | PR существует, `mergeCommit == null` |
| `not_found` | PR/commit доказанно отсутствует в доступном репозитории |
| `no_matching_pr` | commit существует, но не является merge-коммитом связанного PR |
| `actor_unavailable` | merge существует, но `mergedBy` отсутствует или неразрешим |

**Классификация — отдельное поле, а не состояние.** Для `state: merged` с доступным
актором обязательно `actor_class: human | agent | unknown`. «Классифицировано как
unknown» выражается как `state: merged` + `actor_class: unknown`, а **не** шестым
состоянием: иначе `human`/`agent` представлены полем, а `unknown` внезапно типом
результата, и схема становится асимметричной.

**Матрица полей.** `SCHEMA.json` нормативен; таблица — его читаемая форма.
`additionalProperties: false` на каждой ветке, поэтому «не требуется» означает
**запрещено**, а не «необязательно».

| state | `merge_sha` | `identity` | `type_hint` | `actor_class` |
|---|---|---|---|---|
| `merged` | resolved oid | обязательно | обязательно | обязательно |
| `not_merged` | `null` | запрещено | запрещено | запрещено |
| `not_found` | `null` | запрещено | запрещено | запрещено |
| `no_matching_pr` | `null` | запрещено | запрещено | запрещено |
| `actor_unavailable` | resolved oid | запрещено | запрещено | запрещено |

`merge_sha` — **разрешённый** merge-коммит связанного PR, а не эхо запроса. Для
`no_matching_pr` он `null` именно потому, что запрошенный commit таким коммитом не
является: эхо запроса здесь создало бы видимость разрешения.

`state: merged` по определению влечёт разрешённого актора: случай «мерж есть, актора
нет» — это `actor_unavailable`, отдельное состояние.

**Матрица допустимости `state` × `request.kind`.** Не всякое состояние достижимо для
всякого типа запроса, и схема обязана это запрещать, а не полагаться на дисциплину
продюсера:

| state | `request.kind: pr` | `request.kind: merge_sha` |
|---|---|---|
| `merged` | допустимо | допустимо |
| `not_merged` | **только здесь** | запрещено |
| `not_found` | допустимо | допустимо |
| `no_matching_pr` | запрещено | **только здесь** |
| `actor_unavailable` | допустимо | допустимо |

`not_merged` осмысленно только для PR: у запроса по SHA нет состояния «не слит» —
коммит либо является merge-коммитом связанного PR, либо нет (`no_matching_pr`), либо
отсутствует (`not_found`). Симметрично `no_matching_pr` бессмысленно для PR-запроса.

### 4.3 Инварианты валидного файла

Читатель обязан проверить **все**; нарушение любого делает файл `unreadable` целиком,
а не «частично пригодным»:

1. первая и только первая строка — `header`;
2. `scope` непуст и не содержит дублей;
3. для каждого элемента `scope` существует ровно один `result`;
4. `result` вне `scope` запрещён;
5. дубли `result` запрещены;
6. `request` в `result` **семантически равен ровно одному** нормализованному элементу
   `scope` по типизированной идентичности `(kind, value)`. Побайтовое сравнение здесь
   неприменимо: одинаковые по смыслу JSON-объекты различаются порядком ключей и
   пробелами. Где нужна побайтовость — сравниваются канонические байты §4.4, а не
   исходные фрагменты строк JSONL. `merge_sha` остаётся **наблюдаемым полем**, а не
   ключом;
7. поля соответствуют `state`;
8. `generated_at` и `valid_until` — timezone-aware RFC 3339 UTC;
9. **алиасы одного мержа не противоречат друг другу.** `pr:42` и
   `merge_sha:<oid>` могут оба входить в scope и разрешаться в один и тот же merge —
   это законно. Но их наблюдения обязаны совпадать; два расходящихся результата для
   одного разрешённого SHA делают весь файл `unreadable`. Иначе допустимый scope с
   двумя алиасами порождает неоднозначный lookup.
   **Сравниваемая проекция названа явно**, потому что `request` у алиасов различается
   по определению: сравнивается кортеж
   `(state, merge_sha, identity, type_hint, actor_class)` — то есть запись целиком за
   вычетом `request`. Отсутствующие по матрице полей значения считаются `null` с обеих
   сторон, а не «несравнимыми».

   **Известный пробел: инвариант сравнивает только записи с `merge_sha != null`.**
   Запись с `merge_sha: null` (любое из трёх отрицательных состояний — `not_found`,
   `no_matching_pr`, `not_merged`) не участвует в этом сравнении вовсе, потому что у
   неё нет разрешённого SHA, с которым можно было бы сверить другую запись по ключу
   `by_sha`. Из этого следует, что файл вида `pr:42 → merged, merge_sha=X` **вместе с**
   `merge_sha:X → not_found` (`merge_sha: null` по матрице полей §7) — **валиден**:
   обе записи легальны сами по себе, а этот инвариант их не сталкивает. Разрешение
   этого случая на стороне гейта — §8.2/§8.3, а не здесь;


10. **проверки времени, обязательные для ЛЮБОГО читателя** (не только steward):
    `valid_until > generated_at`; оба — UTC; заявленная длительность
    `valid_until − generated_at` ≤ 30 суток (та же граница, что у загрузчика §7);
    `generated_at` более чем на **300 секунд** в будущем — `unreadable`, а не «очень
    свежий файл». Допуск задан **константой контракта**, а не политикой steward:
    его обязан одинаково применять любой потребитель, а нашей политики у него нет.
    Значение нормативно фиксируется в `SCHEMA.json`/`README.md` контракта;
11. **`header.repository` совпадает с наблюдаемым репозиторием.** Инварианты 1–10
    проверяются по файлу самому по себе; этот — единственный, который требует
    внешнего ожидания, и потому обязан быть назван отдельно, а не подразумеваться.
    Нормализованный `header.repository` обязан **точно** равняться ожидаемому
    `owner/repo` читателя (нормализация — та же, что при разборе `origin` в §8.4:
    без учёта регистра, по полной паре, без суффиксного совпадения). Несовпадение —
    файл невалиден по правилу §8.3.1.

    Без этой проверки файл из **другого** репозитория с совпавшим merge SHA прошёл бы
    lookup и повлиял на enforcement. SHA-коллизия здесь не гипотетическая экзотика:
    достаточно форка, зеркала или репозитория с общей историей — merge-коммит с тем же
    oid существует в нескольких репозиториях по построению.

Так усечённый JSONL перестаёт быть валидным префиксом: header обещает полный scope, и
читатель обнаруживает недостающие записи.

### 4.4 `scope_sha256` — корреляционная проверка, не доказательство

`scope_sha256` **не заменяет** биекцию (инварианты 3–5): совпавший хеш не доказывает,
что записи на месте. Его роль — поймать рассогласование заявки до сканирования и
связать файл с конкретным запросом.

Канонические байты определяются нормативно: JSON-массив нормализованных объектов
`request`, отсортированный по `(kind, value)`, сериализация с фиксированными
разделителями `(",", ":")`, без пробелов, UTF-8, без escape non-ASCII. Хеш — sha256 от
этих байтов.

## 5. Продюсер

### 5.1 Механический сбой ⇒ файла нет

Публикация не происходит вовсе при: транспортной ошибке, ошибке авторизации,
**непустом `errors`** в ответе GraphQL, malformed payload, неисчерпанном обходе,
ошибке записи.

Батч публикуется, только если терминальная запись получена **для каждого** элемента
объявленного scope. Файл означает «весь объявленный scope опрошен до определённого
результата», а не «все элементы оказались merged».

### 5.2 Три дефекта текущего продюсера — в обязательном scope работы

1. **`repository or {}`** (`_resolve_by_merge_sha`, `_resolve_by_pr`) схлопывает
   `repository: null` в пустой словарь и поднимает «not found». `repository: null`
   может означать auth/visibility failure и **не может** читаться как отсутствие:
   это механический сбой. Допустимый `not_found` — только когда `repository != null`,
   ошибок нет, а `pullRequest == null`.
2. **`_run_graphql` не смотрит в `errors`** — проверяется лишь код возврата `gh`.
   Любой непустой `errors` — недоступность результата.
3. **Ключ результата — merge SHA.** У неслитого PR SHA нет вовсе, поэтому текущая
   структура физически не может выразить `not_merged`. Результат ключуется **исходным
   запросом**; разрешённый SHA — поле.

### 5.3 Исчерпывающий обход

`associatedPullRequests(first:10)` сегодня идёт **без `pageInfo`**: усечение даже не
детектируется, и при одиннадцати связанных PR продюсер молча ответил бы «совпадения
нет». Под этим контрактом это дало бы **ложный `no_matching_pr`**.

Запрос обязан нести `pageInfo { hasNextPage endCursor }` и обходить страницы до конца.
Потолка нет: любой потолок воспроизводит ту же ложь. Если санитарный предел всё же
нужен, его превышение — механический сбой, а не отрицательный факт.

## 6. Транзакция и свежесть

### 6.1 Delete-before-attempt

1. снять прежнюю публикацию;
2. fsync каталога;
3. выполнить полную материализацию;
4. при успехе опубликовать атомарно: temp в том же каталоге → flush + fsync →
   `os.replace` → fsync каталога;
5. при любом сбое источник остаётся отсутствующим.

Обоснование: отсутствие источника здесь — определённое и **безопасное** состояние
(D6 переводит прогон в `merge_authority: human`). Устаревшее, выглядящее актуальным, —
состояние **опасное**. Удаление перед попыткой превращает опасное в безопасное.

Транзиентный сбой ухудшает доступность, но не создаёт ложного контроля.

### 6.2 Чего delete-before-attempt не решает

Старый файл сохранится, если новая попытка **вообще не стартовала**: планировщик не
сработал, машина выключена, workflow отключён. Поэтому delete-before-attempt нельзя
описывать как полное доказательство свежести. Закрывают это два свойства контракта.

### 6.3 Scope-ограниченность

Actor facts исторического мержа неизменяемы и действуют **только внутри объявленного
scope**. Читатель никогда не трактует файл как «все актуальные мержи»:

- запрошенный PR/SHA есть в scope и имеет result → наблюдение применимо;
- запрошенный PR/SHA отсутствует в scope → `unknown` **независимо от возраста файла**.

Это само по себе исключает применение старого файла к новому мержу.

### 6.4 Lease

Классификация зависит от изменяемой политики, поэтому header несёт `policy_version`,
`policy_digest` и **нормативный срок действия** `valid_until` — lease продюсера, а не
эвристика читателя по возрасту.

После `valid_until`: raw forge facts остаются исторически истинными, но `actor_class` и
файл как operational observation становятся stale; D6 переводит authority в `human`.

Второй marker-файл в v2 не заводится: он дал бы более подробную диагностику попытки, но
missed-run без lease всё равно не решает.

## 7. Конфигурация lease

Ключ в `profiles/approval-policy.yaml` — политика уже аудируема и уже входит в digest,
поэтому изменение lease честно видно как изменение политики.

```yaml
approval_facts_lease_seconds: 86400
```

Требования загрузчика:

- обязательный **положительный int**; `bool` запрещён (как и для `agent_merge_allowed`:
  truthy-скаляр — `PolicyError`, не приведение);
- верхняя граница — **2 592 000 секунд (30 суток)**, зашита в загрузчик; превышение —
  `PolicyError`, не усечение. Граница нужна, чтобы опечатка в порядке величины
  (`864000000` вместо `86400`) не выдала многолетнюю lease, читаемую как свежесть;
- `valid_until == generated_at + approval_facts_lease_seconds`, оба timezone-aware
  RFC 3339 UTC;
- случайное число в схему не зашивается: `SCHEMA.json` требует timestamp, длительность
  приходит из конфига.

Проверки при **чтении** разведены по уровням и перечислены там, где исполняются: то,
что обязан проверить любой читатель контракта (включая dispatcher), — инвариант 10
§4.3; то, что дополнительно проверяет гейт steward, — строки 4–6 §8.3. Точное равенство
заявленной длительности текущему `approval_facts_lease_seconds` — проверка **гейта**, а
не общая: у постороннего читателя нет доступа к нашей политике, и требовать от него
этого нельзя.

`policy_version` в header — значение ключа `version` из того же файла политики
(сегодня `1`); это заявленная версия семантики, а `policy_digest` — доказательство
конкретных байтов. Одно не заменяет другое: версия может не измениться при правке
списков личностей.

`policy_digest` — sha256 по **сырым байтам** `profiles/approval-policy.yaml`. Комментарий
является частью аудируемого артефакта политики, поэтому его изменение честно
инвалидирует evidence.

## 8. Миграция гейта

### 8.1 Гейт остаётся комбинатором

Файл владеет **per-item наблюдением**; гейт комбинирует пять осей:

```
git provenance
  × доступность и свежесть контракта
  × членство в scope
  × терминальный результат
  × действующая политика
```

`agent_merge_allowed` остаётся решением **гейта** по текущей политике. Продюсер не
превращает `actor_class: agent` в вердикт одобрения.

### 8.2 Поиск результата: два разных lookup

Гейт стартует с merge SHA из git-provenance, а результаты ключуются исходным запросом.
Поэтому нужны **два** обращения, и путать их нельзя:

1. **Индекс разрешённых SHA.** Гейт строит `resolved merge_sha → result` по записям,
   у которых `merge_sha != null` (то есть `merged` и `actor_unavailable`). Записи без
   разрешённого SHA в индекс **не входят** — им нечем соответствовать provenance.
2. **Членство в scope по идентичности запроса.** Отдельно проверяется, входит ли
   `{kind: "merge_sha", value: <SHA из provenance>}` в объявленный `scope`.

Развилка по итогам двух проверок:

- есть в индексе → применяется терминальный результат (§8.3);
- в индексе нет, но **в scope есть** → про этот SHA спросили и получили определённый
  отрицательный ответ. Это **противоречие источников**, не «неизвестный актор»: §8.3;
- нет ни там, ни там → мерж вне объявленного scope → `unknown` независимо от возраста
  файла (§6.3).

**Приоритет между проверками — явный: индекс проверяется первым, и попадание в него
решает исход независимо от того, что говорит вторая проверка (scope по идентичности
запроса) о ТОМ ЖЕ SHA.** Это не вытекает из порядка строк таблицы §8.3 само по себе
(строка 6-8 текстуально предшествуют строкам 9-13) — развилка выше решается ДО
входа в таблицу, и таблица §8.3 читается только внутри уже выбранной ветки. Названо
явно здесь, потому что для файла, прошедшего инвариант 9 (см. известный пробел там),
это различимо: запись `pr:42 → merged, merge_sha=X` кладёт `X` в индекс; отдельная
запись `merge_sha:X → not_found` тоже валидна и по чтению строки 7 таблицы «в scope
есть `not_found`» выглядела бы применимой — но она не проверяется вовсе, потому что
`X` уже нашёлся в индексе на первом шаге развилки. Гейт (`_evaluate` в
`gatecheck/approval.py`) реализует именно это: `by_merge_sha().get(sha)` проверяется
первым, и только при `None` код переходит к scope-проверке строк 6-8. Раскрывать этот
случай (например, делать индекс невалидным при наличии противоречащего алиаса) —
решение владельца контракта, а не деталь реализации гейта; см. named-хвост
`approval-facts-index-precedence-over-negative-alias` в `TODO.md`.

### 8.3 Полная таблица: условие контракта → исход гейта

Условия проверяются **в этом порядке**; первое сработавшее определяет исход. Порядок
существенен: поломка прибора не должна читаться как характеристика актора.

**Строки 6-8 против строк 9-13 — не единый линейный проход по всей таблице.** Развилка
§8.2 (индекс vs. scope-по-идентичности vs. ни то ни другое) выбирается **раньше**, чем
таблица применяется, и решает, какое из трёх подмножеств строк (6 / 7-8 / 9-13) вообще
рассматривается для данного SHA. Индекс — первая проверка развилки, поэтому попадание в
него (строки 9-13) исключает строки 7-8 для того же SHA, даже если для него в scope
отдельно заявлен `not_found`/`no_matching_pr` через другой алиас запроса (см. известный
пробел инварианта 9 в §4.3 и хвост `approval-facts-index-precedence-over-negative-alias`
в `TODO.md`). Строки 1-5 и строка 6 читаются линейно как написано.

| # | условие | класс | исход |
|---|---|---|---|
| 1 | файла нет по разрешённому пути | `unavailable` | finding: evidence absent — материализовать `steward approval-facts` |
| 2 | файл невалиден: legacy `approval-facts/v1`, не разбирается, нарушен любой инвариант §4.3 | `unavailable` | по правилу §8.3.1 — finding для bundle-default, config error exit 2 для явного пути |
| 3 | `policy_digest` ≠ sha256 текущих байтов `profiles/approval-policy.yaml` | `unavailable` | finding: наблюдение получено по другой политике. **Приоритет выше любого result и выше живой lease** |
| 4 | заявленная длительность ≠ текущему `approval_facts_lease_seconds` | `unavailable` | finding: lease не соответствует действующей конфигурации |
| 5 | `now >= valid_until` | `unavailable` (stale) | finding: наблюдение просрочено |
| 6 | SHA нет ни в индексе, ни в scope как `{kind:"merge_sha"}` | `unknown` | finding: мерж вне объявленного scope |
| 7 | `{kind:"merge_sha", value:<SHA>}` в scope, результат `not_found` | `unavailable` | finding: **противоречие источников** — git предъявляет merge-provenance для SHA, форж заявляет его отсутствие. Отдельная диагностика, fail-closed |
| 8 | `{kind:"merge_sha", value:<SHA>}` в scope, результат `no_matching_pr` | `unavailable` | finding: **противоречие источников** — SHA существует, но не является merge-коммитом связанного PR, при том что локально предъявлен как merge-provenance |
| 9 | `state: actor_unavailable` (SHA в индексе) | `unavailable` | finding: мерж есть, актор неразрешим |
| 10 | `state: merged` + `actor_class: unknown` | `unknown` | finding: актор не опознан закрытой классификацией |
| 11 | `state: merged` + `actor_class: agent`, `agent_merge_allowed: false` | agent denied | finding |
| 12 | `state: merged` + `actor_class: agent`, `agent_merge_allowed: true` | agent allowed | finding нет |
| 13 | `state: merged` + `actor_class: human` | human | finding нет |

**Почему `not_merged` в таблице отсутствует.** Он достижим только для запроса
`kind: pr` (§4.2) и несёт `merge_sha: null`, поэтому в индекс разрешённых SHA не входит
и связать его с локальной merge-provenance **нечем**. Строка про него была бы обещанием,
которое реализация выполнить не может. Если форж считает PR неслитым, а локально
merge-provenance есть, то соответствующий SHA просто не найдётся ни в индексе, ни в
SHA-scope — и это строка 6, `unknown` вне объявленного scope, а не доказанное
противоречие. Противоречием считается только расхождение по **тому же самому merge SHA**
(строки 7–8).

### 8.3.1 Правило для невалидного файла: путь решает класс ошибки

Один принцип вместо частных случаев:

- невалидный файл по **bundle-default** — источник `unreadable`, finding. Никто не
  указывал на этот файл руками: он просто оказался таким, и это наблюдаемое свойство
  среды;
- невалидный файл по **явно выбранному оператором** пути (`--approval-facts`) —
  **config error, exit 2**. Оператор указал не тот файл, и это ошибка вызова, а не
  свойство наблюдения.

Граница проходит по **валидности файла**, не по свежести. Просроченная lease,
несовпавший `policy_digest` и несоответствие длительности — свойства **корректного**
файла, который честно сообщает о себе; они дают finding на обоих путях. Config error —
только когда файл не является тем, за что себя выдаёт: не разбирается, легаси, нарушает
инварианты.

`agent_merge_allowed` остаётся решением **гейта** по текущей политике (§8.1); продюсер
не превращает `actor_class: agent` в вердикт одобрения.

### 8.4 Пути ввода и разрешение целевого пути

Один формат, два явно выбранных пути:

- `steward approval-facts` по умолчанию пишет `<repo-root>/.steward/approval_facts.jsonl`;
  `--out <path>` — явный override для тестов, offline-материализации и приёмки;
- `gate-check` по умолчанию читает путь бандла; `--approval-facts <path>` — явный
  override, принимающий **только v2**.

Без согласования ad-hoc `--out` порождал бы файл, который собственный гейт steward не
умеет использовать.

**Откуда берётся `<repo-root>`.** Из `--repo owner/name` он не выводится: это slug на
форже, а не путь на диске. Вводится `--repo-root` со значением по умолчанию `.`.

При `--out` транзакция §6.1 применяется к указанному пути и его родительскому каталогу;
проверка соответствия remote не выполняется — override на то и override, но и путь
бандла он тогда не трогает.

#### Разбор `origin` — нормативно

Сравнивается remote с именем **`origin`** (не «любой remote, который подойдёт»): в
рабочих копиях флота встречаются несколько remote'ов на один репозиторий — у
`atp-platform` их три, включая зеркало на чужом форже. Отсутствие `origin` — config
error.

Разбор: принимаются формы `git@github.com:owner/repo.git`,
`ssh://git@github.com/owner/repo.git`, `https://github.com/owner/repo.git` и та же без
`.git`. Хвостовой `.git` отбрасывается, извлекается **ровно** `owner/repo`.

Сопоставление — по полной паре `(owner, repo)`, **без суффиксного сравнения**:
`endswith("owner/repo")` совпал бы с `other-owner/repo` и с чужим хостом. Регистр
игнорируется, потому что форж трактует эти имена без учёта регистра; всё остальное —
точное равенство.

#### Полный config preflight до удаления

Delete-before-attempt (§6.1) начинается **только после того, как проверены все входы**,
а не только цель. До первого разрушающего действия обязаны пройти:

1. разбор и валидация `--repo`;
2. **для bundle-target** — разрешение `--repo-root` до корня git-репозитория и сверка
   `origin` (выше). **При `--out` сверка `origin` пропускается**, поскольку override по
   определению адресует произвольный путь, не обязанный лежать в чекауте наблюдаемого
   репозитория; но сам путь override всё равно разрешается и валидируется (существует
   родительский каталог, он записываем) **до** удаления. Пропуск сверки безопасен
   именно потому, что читатель обязан проверить `header.repository` (инвариант 11
   §4.3): подмена репозитория ловится на чтении, а не только на записи;
3. валидация объявленного scope: непуст, без дублей, идентификаторы правильной формы;
4. загрузка политики и вычисление `policy_digest`;
5. валидация `approval_facts_lease_seconds` (§7), включая верхнюю границу.

Любая ошибка на этих шагах — config error, exit 2, **прежняя публикация не тронута**.

Причина именно такая, как в §6.1, но с другой стороны: удаление превращает устаревшее
наблюдение в отсутствующее осознанно, ради безопасности. Удалять же его из-за опечатки
в конфиге — не осознанный размен, а потеря данных на ровном месте. Разрушающее действие
допустимо только тогда, когда всё остальное уже доказано.

## 9. Приёмка

### 9.1 Регрессия (обязательный набор)

1. provenance absent;
2. evidence file absent;
3. malformed / incomplete / stale;
4. мерж вне scope;
5. actor unavailable;
6. classified unknown;
7. human;
8. agent denied;
9. agent allowed;
10. стадия `authoring` не запускает approval-гейт вовсе;
11. артефакт вне default branch остаётся вне охвата.

Добавлено правками дизайна (та же обязательность):

12. legacy v1 по пути бандла — finding «unsupported legacy»; он же по явному
    `--approval-facts` — config error, exit 2;
13. `policy_digest` не совпал при **живой** lease — приоритет digest, fail-closed;
14. заявленная длительность ≠ текущему `approval_facts_lease_seconds`;
15. `generated_at` в будущем сверх clock-skew — `unreadable`, не «свежий»;
16. противоречие источников по **тому же merge SHA**: результат `not_found` и
    результат `no_matching_pr` (два случая, два разных сообщения). Плюс контрольный:
    PR-запрос с `not_merged` при существующей локальной merge-provenance даёт
    `unknown` вне scope, **а не** противоречие — связать их нечем;
17. схема отвергает недопустимые пары `state` × `request.kind`: `not_merged` для
    `merge_sha`, `no_matching_pr` для `pr`;
18. алиасы одного мержа (`pr:N` и `merge_sha:<oid>`) согласуются по кортежу
    §4.3(9) — файл валиден; они же расходятся — весь файл `unreadable`;
19. `--repo-root` не соответствует `--repo` по `origin` — config error, exit 2, и
    **прежняя публикация не тронута**; отдельно: `origin` отсутствует; отдельно:
    суффиксное совпадение (`other-owner/repo`) НЕ принимается;
20. каждый шаг config preflight §8.4 роняет прогон до удаления: битый scope, битая
    lease-конфигурация, нечитаемая политика — прежняя публикация цела;
21. невалидный файл по bundle-default — finding; он же по явному `--approval-facts` —
    config error exit 2 (правило §8.3.1);
22. просроченная lease по явному пути остаётся **finding**, а не config error:
    граница §8.3.1 проходит по валидности файла, не по свежести;
23. `header.repository` не совпадает с наблюдаемым репозиторием — файл невалиден
    (finding по bundle-default, config error по явному пути). Контрольный случай:
    файл из другого репозитория, содержащий тот же merge SHA, **не** влияет на
    enforcement;
24. `--out` не требует сверки `origin` и работает вне чекаута наблюдаемого репозитория,
    но нечитаемый родительский каталог override-пути роняет прогон **до** удаления.

### 9.2 Migration acceptance на реальных мержах

На мержах live-прогона 2026-08-20: `e04b0c9` (человек, `github:andrei-shtanakov`,
`User`) и `2214579` / `05aa16e` (`github:merge-broker`, `Bot`). Результаты обязаны
остаться **эквивалентными по enforcement**, но пройти через scope, lease и терминальные
состояния v2.

## 10. Хвосты, не входящие в этот воркстрим

- `verdicts/emitter.py` пишет `gate_verdicts.jsonl` через temp + `os.replace` **без
  fsync** — слабее требований §6.1. Отдельный TODO, не расширяющий эту работу.
- Стадия 2 dispatcher: вендоринг пиненой копии `contracts/approval-facts/v2/` +
  `core/merge_actor.py` по образцу `core/governance.py` + тесты.
