=== PROMPT ===
# Task Execution Request

## Task: TASK-001 — Extract facts from source of truth

**Priority:** P0
**Estimate:** 
**Milestone:** 

## Checklist (implement ALL items):

- [ ] Read `src/steward/riskclassify/cli.py`: exact flag names and defaults (`--declared`, `--no-fs`, `--diff`, `--risk-model` default `profiles/risk-model.yaml`, `--profile` default `lite`, `--repo`, `--project`, `--sha`), exit codes for both commands, `waivers-check [DIR]` default directory (`spec/waivers`), missing-directory behavior.
- [ ] Read `src/steward/riskclassify/cli.py` `_read_json` (and related helpers): required/optional keys for the `--declared` schema (`project`, `sha`, `scope`, optional `flags`) and the `--no-fs` schema (`project`, `sha`, `paths`, optional `declared_scope`, `flags`).
- [ ] Read `src/steward/riskclassify/classify.py` and `model.py`: the nine `Classification` output fields and their meanings; tier values (`low`/`medium`/`high`/`critical`); phases (`ex_ante`/`ex_post`); the three axes; max-of-floor-and-axes rule; deterministic sorted-key JSON output.
- [ ] Read `src/steward/riskclassify/waivers.py`: the five frontmatter fields (`gate_id`, `sha`, `tier`, `waived_by`, `reason`), SHA-bound/stale semantics, rule ids (`waiver-stale-sha`, `waiver-forbidden-tier`), critical-tier prohibition, finding categories behind exit code 1.
- [ ] Skim `workstreams/WS-006-risk-model/spec/design.md` (DESIGN-601…610) to confirm terminology and cross-check the above.
- [ ] Record collected facts (flag names, defaults, field names, rule ids, exit codes) as notes for TASK-002; flag any discrepancy between design doc and code (code wins per [DESIGN-012]).

## Related Requirements:

See /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/requirements.md

## Related Design:

See /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/design.md

## Instructions:

1. Implement ALL checklist items for this task
2. Write unit tests for new code (coverage ≥80%)
3. Follow the design patterns from /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/design.md
4. Use existing code style and conventions
5. Create/update files as needed

## Dependencies:

- To add a new dependency: `uv add <package>`
- To add a dev dependency: `uv add --dev <package>`
- NEVER edit pyproject.toml manually for dependencies
- After adding dependencies, they are available immediately

## Success Criteria:

- All checklist items implemented
- All tests pass (`uv run pytest`)
- No lint errors (`uv run ruff check .`)
- Code follows project conventions

## Output:

When complete, respond with:
- Summary of changes made
- Files created/modified
- Any issues or notes
- "TASK_COMPLETE" if successful, or "TASK_FAILED: <reason>" if not


Begin implementation:


=== OUTPUT ===
## Summary

TASK-001 was a read-only fact-extraction task. I read all four source files (`cli.py`, `classify.py`, `model.py`, `waivers.py`) and the WS-006 design doc, and recorded every fact TASK-002 needs in a notes file.

**Facts collected and confirmed against code:**
- `risk-classify` flags/defaults: `--diff` / `--no-fs` / `--declared` (exactly one required), `--risk-model` default `profiles/risk-model.yaml`, `--profile` default `lite`, `--repo` default `.`, `--project` (diff-only, defaults to repo dir name). Exit codes `0`/`2`, no exit 1.
- `_read_json` schemas: `--declared` requires `project`/`sha`/`scope` (+optional `flags`); `--no-fs` requires `project`/`sha`/`paths` (+optional `declared_scope`, `flags`); type checks map to exit 2.
- The nine `Classification` fields, tiers `low`/`medium`/`high`/`critical`, phases `ex_ante`/`ex_post`, the three axes, the max-of-floor-and-axes combinator, `scope_violation` floor at `high`, and sorted-key deterministic JSON output.
- `waivers-check [DIR]` default `spec/waivers`, missing dir is clean (exit 0), the five required frontmatter fields, SHA-bound/stale semantics, and all rule ids behind exit 1: `waiver-bad-sha`, `waiver-bad-tier`, `waiver-forbidden-tier`, `waiver-stale-sha`, plus load-time `waiver-malformed`.

**Design-doc discrepancies flagged (code wins per DESIGN-012):** the WS-006 doc lists only four waiver fields (code requires five, adding `tier`); DESIGN-610 shows a nonexistent `--format json` flag; it renders `--no-fs` as an optional extra rather than a third exclusive mode; and the `<gate_id>-<shortsha>.md` waiver naming is convention, not enforced.

**Two important discoveries for downstream tasks:**
1. **`docs/risk-classify.md` already exists and is git-tracked** (90 lines, from a prior run). I cross-checked it line-by-line — it is fully accurate against the code and contains none of the design-doc errors. TASK-002 should verify/align rather than create; it may need no edit at all.
2. **Pre-existing test failure:** `tests/test_meta.py::test_all_dogfood_specs_are_managed` fails because the maestro commit `99f9751` added this workstream's `spec/*.md` files without frontmatter. It predates this task; fixing it would violate REQ-001. 122/123 tests pass; `ruff check` is clean.

## Files created/modified

- Created: `.claude/TASK-001-facts.md` — the TASK-002 handoff notes. Placed under `.claude/` because it is gitignored, so the committed deliverable stays exactly one file per REQ-001. No tracked file was touched (`git status` shows only the executor's own state files, unchanged).

## Notes

- No code was written, so the unit-test boilerplate does not apply to this task.
- Transient mishap, fully undone: a `git stash -u` briefly stashed the executor's untracked state files; `git stash pop` restored them and the working tree matches its starting state.

TASK_COMPLETE

=== STDERR ===


=== RETURN CODE: 0 ===
