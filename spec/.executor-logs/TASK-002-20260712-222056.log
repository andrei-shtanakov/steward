=== PROMPT ===
# Task Execution Request

## Task: TASK-002 — Author part 1: `steward risk-classify` section

**Priority:** P0
**Estimate:** 
**Milestone:** 

## Checklist (implement ALL items):



## Related Requirements:

See /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/requirements.md

## Related Design:

See /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/design.md

## Instructions:

1. Implement ALL checklist items for this task
2. Write unit tests for new code (coverage ≥80%)
3. Follow the design patterns from /private/tmp/claude-501/-Users-Andrei-Shtanakov-labs-all-ai-orchestrators/c24dfd0f-0a40-416e-ac6f-f751e22d5181/scratchpad/governed-run2/ws/risk-classify-docs/spec/design.md
4. Use existing code style and conventions
5. Create/update files as needed

## Dependencies:

- To add a new dependency: `uv add <package>`
- To add a dev dependency: `uv add --dev <package>`
- NEVER edit pyproject.toml manually for dependencies
- After adding dependencies, they are available immediately

## Success Criteria:

- All checklist items implemented
- All tests pass (`uv run pytest`)
- No lint errors (`uv run ruff check .`)
- Code follows project conventions

## Output:

When complete, respond with:
- Summary of changes made
- Files created/modified
- Any issues or notes
- "TASK_COMPLETE" if successful, or "TASK_FAILED: <reason>" if not


Begin implementation:


=== OUTPUT ===
TASK-002 is complete. I created `docs/risk-classify.md` containing the intro and the full part 1 (`steward risk-classify`) section, grounded directly in the verified source modules.

**Changes made** (one new file, `docs/risk-classify.md`, 62 lines — nothing else touched):

- `# Risk classification CLI` intro naming the two WS-006 M1 commands, per the DESIGN-002 skeleton.
- Purpose paragraph: two-phase tiering (`ex_ante` over a declared scope / `ex_post` over a diff), tier = `max(profile floor, change_class, blast_radius, trust_boundary)`, deterministic JSON on stdout, and the "consumers read the output, never compute risk" framing (REQ-004).
- `### Input modes`: all three modes with exactly one fenced example each — `--declared` (JSON with `project`/`sha`/`scope`, optional `flags`), `--no-fs` (JSON with `project`/`sha`/`paths`, optional `declared_scope`/`flags`, plus the `scope_violation` ⇒ tier ≥ `high` note), and `--diff BASE..HEAD` as a bash invocation. Includes the "exactly one mode" note and the `--risk-model` / `--profile` defaults (REQ-005).
- `### Output`: table covering all nine fields (`tier`, `phase`, `inputs`, `dominant_axis`, `floor_profile`, `mandatory_gates`, `flags`, `sha`, `risk_model_version`) plus the byte-stable-output sentence (REQ-006).
- `### Exit codes`: `0` (any tier, including `critical` — classification is a function, not a check) and `2` (config error); no nonexistent exit 1 documented (REQ-007, REQ-014).

**Verification:** every field name, JSON key, default, and exit code was checked against `cli.py`, `classify.py`, and `model.py` (e.g. the nine `Classification` dataclass fields, `_DEFAULT_MODEL = profiles/risk-model.yaml`, `profile="lite"`, `_SCOPE_VIOLATION_MIN = "high"`, `version_sha = "sha256:…"`). `uv run pytest` passes (123 passed) and `uv run ruff check .` is clean. The working tree contains exactly the one new file.

**Notes:** No unit tests were added because this task is documentation-only — there is no new code to cover. Part 2 (`steward waivers-check`) is deliberately absent; it is TASK-003's scope in the same file. Part 1 was kept to 62 lines so the finished doc can stay within the ≈60–90-line one-page target after part 2 lands.

TASK_COMPLETE

=== STDERR ===


=== RETURN CODE: 0 ===
