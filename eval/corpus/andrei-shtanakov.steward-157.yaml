schema: review-eval-case/v1
case_id: andrei-shtanakov.steward-157
repo: andrei-shtanakov/steward
pr: 157
base_sha: b84a3bbb3ef9f4c9c23194e16e94d6c4836f79a9
head_sha: ffa6f0f986cd8861ec31c75567543cce1df645c3
class: clean
expected_outcome: verdict
annotation:
  status: adjudicated
  blocking_complete: true
  source: history-proxy
  adjudicated_by: github:andrei-shtanakov
  adjudicated_at: 2026-09-18
defects:
- id: D-andrei-shtanakov.steward-157-1
  severity: minor
  file: tests/review/test_collect_context.py
  line_hint: 613
  scenario: 'Docstring теста утверждает механизм (`-c core.quotePath=false`), которого
    в скрипте нет — используется `ls-tree -z` (confidence: high). Мейнтейнер читает
    `test_non_ascii_path_resolves_to_itself` как объяснение, почему не-ASCII путь
    проходит структурную проверку: docstring говорит «Оба вызова ls-tree идут с `-c
    core.quotePath=false`, как уже делает local.sh». В итоговом дереве ни один вызов
    `ls-tree` в collect-context.sh этот флаг не передаёт — механизм заменён на `-z
    | tr ''\0'' ''\n''` (коммит 2971726), и комментарий скрипта в строках 103–109
    прямо объясняет, почему `core.quotePath=false` НЕ достаточно (`"` и `\` квотируются
    всегда). Соседний тест (строка 629–633) описывает актуальный механизм верно; первый
    — устаревшую версию.'
  evidence:
  - tests/review/test_collect_context.py:613
  - scripts/review/collect-context.sh:110
  - scripts/review/collect-context.sh:219
  - scripts/review/collect-context.sh:103
  - tests/review/test_collect_context.py:631
  match:
    files:
    - tests/review/test_collect_context.py
    line_window: 40
    keywords_any:
    - используется
    - утверждает
    - docstring
    - quotepath
    - механизм
non_defects:
- id: NF-andrei-shtanakov.steward-157-1
  file: scripts/review/collect-context.sh
  line_hint: 228
  scenario: 'ЛОЖНАЯ находка (отклонена при адъюдикации 2026-09-18, проверено эмпирически).
    Утверждение: обратная косая в пути — glob-спецсимвол pathspec, поэтому при наличии соседа
    без `\` `ls-tree` вернёт ДВЕ записи и легитимный путь будет отвергнут как «разрешился
    не в себя». Не воспроизводится: на git 2.54.0 в дереве с обоими `docs/back\slash.md` и
    `docs/backslash.md` запрос `ls-tree -z --full-tree <base> -- ''docs/back\slash.md''` даёт
    РОВНО ОДНУ запись — литеральный файл; сквозной прогон collect-context.sh в этом же сценарии
    выходит кодом 0 и прикладывает верное содержимое. Цепочка рвётся на первом звене, поэтому
    повтор этого утверждения — известный ложный класс, а не находка.'
  match:
    files:
    - scripts/review/collect-context.sh
    line_window: 40
    keywords_any:
    - спецсимвол
    - легитимный
    - разрешился
    - отвергнут
    - обратная
notes: |-
  Черновик из истории ревью (source: history-proxy): andrei-shtanakov/steward#157, ревью ai-prosto id=5200707170 от 2026-09-14T17:16:34Z.
  Ревью ai-prosto на PR: 1; находок в последнем: 2.
  head_sha взят из маркера ревью; merge_commit_sha: 57170da804ecd638cc3d8b5184c3c87f93bf99a1.
  Коммитов PR после последнего ревью: 0.
  candidate_status (подсказка D1, не ground truth):
    - D-andrei-shtanakov.steward-157-1: unknown [minor] tests/review/test_collect_context.py:613
    - D-andrei-shtanakov.steward-157-2: unknown [minor] scripts/review/collect-context.sh:228
  Адъюдикация 2026-09-18 (разметчик github:andrei-shtanakov). Диапазон прочитан
  целиком: 3 файла, collect-context.sh — построчно.
  D-andrei-shtanakov.steward-157-1 ПОДТВЕРЖДЁН, класс minor: докстринг НОВОГО теста
  обещает механизм `-c core.quotePath=false`, которого в скрипте нет (реально `ls-tree
  -z | tr`), а соседний тест описывает настоящий механизм верно. Дефект внесён этим же
  патчем. Не major: неверно только объяснение, проверяемое поведение теста корректно.
  D-andrei-shtanakov.steward-157-2 ОТКЛОНЁН как ложная находка, перенесён в non_defects
  под NF-andrei-shtanakov.steward-157-1. Проверено эмпирически на git 2.54.0: в дереве,
  где лежат ОБА `docs/back\slash.md` и `docs/backslash.md`, запрос `ls-tree -z
  --full-tree <base> -- 'docs/back\slash.md'` возвращает РОВНО ОДНУ запись —
  литеральный файл, а не две; сквозной прогон collect-context.sh в точно этом сценарии
  (манифест называет путь с обратной косой, сосед без неё существует) выходит кодом 0 и
  прикладывает правильное содержимое. Цепочка находки рвётся на первом же звене
  («ls-tree вернёт две записи»), поэтому это не gold-дефект, а известный ложный класс.
  blocking_complete: true.
